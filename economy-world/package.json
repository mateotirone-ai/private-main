{
  "name": "economy-world",
  "version": "0.4.0",
  "private": true,
  "description": "Economy World - Minecraft Bedrock add-on. Phase D: work engines + employment.",
  "type": "module",
  "scripts": {
    "build": "node tools/build.mjs",
    "watch": "node tools/build.mjs --watch",
    "test": "vitest run",
    "deploy": "node tools/deploy.mjs"
  },
  "devDependencies": {
    "@minecraft/server": "2.9.0",
    "@minecraft/server-ui": "^2.1.0",
    "esbuild": "^0.24.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  }
}
